{
  "name": "amul-animated-website",
  "version": "1.0.0",
  "description": "Amul Scroll Animation website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "mkdir -p dist && cp index.html style.css app.js dist/ && cp -r frames dist/"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
